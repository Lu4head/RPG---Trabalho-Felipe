// Arquivo para importação dos itens criados
#pragma once // pragma once para não espelhar os arquivos importados

#include "Arma.h"
#include "Pocao.h"

extern Arma* armas_inciais[]; // Array com as armas inicias
extern Item* itens_nivel_1[]; // Array com as armas de nível 1
extern Item* itens_nivel_2[]; // Array com as armas de nível 2
extern Item* itens_nivel_3[]; // Array com as armas de nível 3
extern Item* itens_nivel_4[];
extern Item* itens_nivel_5[];
extern Item* itens_nivel_6[];
extern Item* itens_nivel_7[];
extern Item* itens_nivel_8[];


extern int tamanho_itens_nivel_1; // Tamanho do array de armas de nível 1
extern int tamanho_itens_nivel_2; // Tamanho do array de armas de nível 2
extern int tamanho_itens_nivel_3; // Tamanho do array de armas de nível 3
extern int tamanho_itens_nivel_4;
extern int tamanho_itens_nivel_5;
extern int tamanho_itens_nivel_6;
extern int tamanho_itens_nivel_7;
extern int tamanho_itens_nivel_8;
